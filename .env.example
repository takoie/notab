# Written automatically by `npx convex dev` into .env.local — do not fill these by hand.
# Convex deployment URL (WebSocket + function calls)
VITE_CONVEX_URL=https://your-deployment.convex.cloud
# Convex HTTP actions / file storage
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site
